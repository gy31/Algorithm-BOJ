#include<stdio.h>
int A[10000];
int main() {
    int i, j, n, m, cnt = 0;
    scanf("%d%d", &n, &m);

    scanf("%d", &A[0]);
    for(i = 1; i < n; i++) {
        int tmp;
        scanf("%d", &tmp);
        A[n] = 
    }

    for(i = 0; i < n; i++) {
        
    }

    return 0;
}