#include<stdio.h>
#include<queue>
#include<algorithm>
using namespace std;
int main() {
    int i, n, x;
    priority_queue<int, vector<int>, greater<int>> pq;

    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d", &x);
        pq.push()
    }

    return 0;
}